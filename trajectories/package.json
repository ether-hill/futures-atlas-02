{
  "name": "trajectories",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futures Atlas — Trajectories. A reimplementation (homage) of Jeongho Park's 'Collective Trajectories' (CC BY-NC 4.0): luminous filaments radiating from the centre of a sphere to its surface, with brightness pulses flowing outward and ripples spreading at the shell.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
