{
  "name": "mappings",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futures Atlas — Mappings. A Mapping-Police-Violence-style evidence site where the data domain is a variable: each mapping is a dataset module (records + dimensions + sources) rendered by one shared set of map/chart/filter widgets.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "harvest": "node scripts/harvest.mjs"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  },
  "devDependencies": {
    "@types/d3-geo": "^3.1.0",
    "@types/topojson-client": "^3.1.5",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
