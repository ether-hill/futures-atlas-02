{
  "name": "hyperscale",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Futures Atlas — Hyperscale. A 3D data-center builder: place server halls, cooling towers and substations on a terrain plot and balance power, heat, compute and cash as demand for AI compute explodes.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
