{
  "name": "prism",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futures Atlas — Generatives. A generative-visual lab: an array of animated, embeddable treatments for the quantum-computing futures visual language.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "simplex-noise": "^4.0.3",
    "three": "^0.171.0",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "@types/three": "^0.171.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
