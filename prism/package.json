{
  "name": "prism",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futures Atlas — Prism. A generative-visual lab: a smorgasbord of animated, embeddable treatments for the quantum-computing futures visual language.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "simplex-noise": "^4.0.3",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
