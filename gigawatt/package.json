{
  "name": "gigawatt",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futures Atlas — Gigawatt. A 3D AI compute-campus sim in the high desert: mix grid, solar and battery power, keep GPU halls cool through heat waves and dust storms, manage a shrinking aquifer, and chase training contracts as spot compute prices boom.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
