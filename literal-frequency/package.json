{
  "name": "literal-frequency",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futures Atlas — Literal Frequency. Word-frequency visualisations built from books in the Source Library (sourcelibrary.org), each linking back to its source.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
